import React from 'react';
import { NavLink } from 'react-router-dom';

const Navbar = () => {
  return (
    <nav>
      <ul>
        {/* Single NavLink for /reg1 and /reg2 */}
        <li>
       
        </li>
      </ul>
    </nav>
  );
};

export default Navbar;
